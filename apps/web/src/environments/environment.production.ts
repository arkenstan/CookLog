/** Replaced for production builds via angular.json fileReplacements. Fill in before deploying. */
export const environment = {
  production: true,
  supabase: {
    url: 'https://YOUR-PROJECT-REF.supabase.co',
    key: 'YOUR-PUBLISHABLE-KEY',
  },
};
