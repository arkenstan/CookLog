{
  "name": "cooklog",
  "private": true,
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "gen:types": "supabase gen types typescript --local > packages/data-access/src/lib/database.types.ts",
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "db:test": "supabase test db"
  },
  "devDependencies": {
    "prettier": "^3.6.0",
    "supabase": "^2.117.0",
    "turbo": "^2.5.0"
  }
}
